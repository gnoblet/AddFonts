# Sentinel provider for direct-URL font downloads (provider = "url")

Returned internally when `add_font(provider = "url")` is used. Carries
no extra properties — its type alone signals the direct-URL dispatch
path.

## Usage

``` r
FontProviderDirectURL()
```
