# Sentinel provider for local font files (provider = "file")

Returned internally when `add_font(provider = "file")` is used. Carries
no extra properties — its type alone signals the local-copy dispatch
path.

## Usage

``` r
FontProviderLocal()
```
