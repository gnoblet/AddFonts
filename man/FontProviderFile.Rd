% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FontProviderFile.R
\name{FontProviderFile}
\alias{FontProviderFile}
\title{File-based font provider (FontProviderFile)}
\usage{
FontProviderFile(
  source,
  base_url,
  file_ext = "ttf",
  aliases = list(),
  first_use_message = NULL,
  first_use_url = NULL
)
}
\arguments{
\item{source}{(\code{character(1)}) Provider id/name (e.g. \code{"bbb"}).}

\item{base_url}{(\code{character(1)}) Glue-style URL template. Must contain \code{{family}} and \code{{filename}}
placeholders (e.g.
\code{"https://gitlab.com/bye-bye-binary/{family}/-/raw/main/ttf/{filename}.ttf"}).}

\item{file_ext}{(\code{character(1)}) Extension of the font files served by this provider (default: \code{"ttf"}).
Must be \code{"ttf"} or \code{"otf"} (no conversion is performed).}

\item{aliases}{(\code{list}) Optional alias strings (inherited from \code{\link[=FontProvider]{FontProvider()}}).}

\item{first_use_message}{(\code{character(1) | NULL}) Inherited from \code{\link[=FontProvider]{FontProvider()}}.}

\item{first_use_url}{(\code{character(1) | NULL}) Inherited from \code{\link[=FontProvider]{FontProvider()}}.}
}
\value{
(\code{FontProviderFile}) A validated S7 \code{FontProviderFile} object.
}
\description{
Provider that downloads font files directly by filename from a base URL, with no weight/style/subset parameterisation. Covers Git-hosted collections like Bye Bye Binary where each variant has a fixed filename.
}
