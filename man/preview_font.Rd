% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview_font.R
\name{preview_font}
\alias{preview_font}
\title{Preview a font by ensuring it's installed and drawing a sample string}
\usage{
preview_font(
  name,
  provider = "bunny",
  family = NULL,
  sample = "The quick brown fox jumps over the lazy dog",
  size = 28,
  subset = "latin",
  regular.wt = 400,
  bold.wt = 700
)
}
\arguments{
\item{name}{(\code{character(1)}) Font name as used by the provider (e.g. "oswald").}

\item{provider}{(\code{character(1)}) Provider name to use (default: "bunny")}

\item{family}{(\code{character | NULL}) Optional family name to register the font under (default: NULL)}

\item{sample}{(\code{character(1)}) Sample text to display (default: "The quick brown fox jumps over the lazy dog")}

\item{size}{(\code{numeric(1)}) Font size in points for the preview (default: 28)}

\item{subset}{(\code{character(1)}) Glyph subset to request (default: "latin")}

\item{regular.wt}{(\code{integer(1)}) Regular weight to display (default: 400)}

\item{bold.wt}{(\code{integer(1)}) Bold weight to display (default: 700)}
}
\value{
(\code{list}) Invisibly returns the list of paths produced by \code{add_font()}.
}
\description{
Ensure the requested font is installed via \code{add_font()} and draw a brief
sample using \code{showtext} for proper font rendering.
}
