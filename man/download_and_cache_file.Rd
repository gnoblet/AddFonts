% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_and_cache_file.R
\name{download_and_cache_file}
\alias{download_and_cache_file}
\title{Download all variants of a file-based font and add to cache}
\usage{
download_and_cache_file(
  provider,
  name,
  family_name,
  variants,
  cache_dir = NULL
)
}
\arguments{
\item{provider}{(\code{FontProviderFile}) A file-based provider object.}

\item{name}{(\code{character(1)}) Font name as known to the provider (used in URL template as \code{{family}}).}

\item{family_name}{(\code{character(1)}) Family name under which to register the font.}

\item{variants}{(\code{list}) Named list mapping symbolic variant keys to filename stems. Names must be a subset of \code{c("regular", "italic", "bold", "bolditalic")}. At minimum, \code{"regular"} must be present. Values are filename stems without extension (e.g. \code{list(regular = "Alpaga-Regular", bold = "Alpaga-Bold")}).}

\item{cache_dir}{(\code{character | NULL}) Cache directory to use (default: NULL).}
}
\value{
(\code{CacheEntry | NULL}) Cache entry with downloaded variants, or \code{NULL} if the regular variant could not be downloaded.
}
\description{
Downloads each named variant from a \code{FontProviderFile} provider, creates a \code{\link[=CacheEntry]{CacheEntry()}} with symbolic keys (\code{"regular"}, \code{"italic"}, \code{"bold"}, \code{"bolditalic"}), writes the entry to the cache, and returns it.
}
