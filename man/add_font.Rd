% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_fonts.R
\name{add_font}
\alias{add_font}
\title{Add Fonts from Multiple Providers}
\usage{
add_font(
  name,
  provider = "bunny",
  family = NULL,
  regular.wt = 400,
  bold.wt = 700,
  italic = TRUE,
  ...
)
}
\arguments{
\item{name}{Character. The font name as listed by the provider.}

\item{provider}{Character. Font provider to use. Currently supports:
\itemize{
\item \code{"bunny"}: Bunny Fonts (default) - privacy-focused Google Fonts alternative
}}

\item{family}{Character. The family name to register in R. Defaults to \code{name}.}

\item{regular.wt}{Integer. The weight of the regular font variant (default: 400).}

\item{bold.wt}{Integer. The weight of the bold font variant (default: 700).}

\item{italic}{Logical. Whether to download italic variant (default: TRUE if available).}

\item{...}{Additional provider-specific arguments.}
}
\value{
Invisibly returns a list of paths to the registered font files.
}
\description{
A general function to download and register fonts from various providers.
Currently supports Bunny Fonts, with more providers planned for the future.
}
\examples{
\dontrun{
# Add a font from Bunny Fonts (default provider)
add_font("roboto")

# Explicitly specify provider
add_font("open-sans", provider = "bunny")

# Customize font properties
add_font("inter", family = "Inter", regular.wt = 300, bold.wt = 600)

# Enable showtext and use the font
showtext::showtext_auto()
plot(1:10, main = "Using fonts from multiple providers!")
}
}
