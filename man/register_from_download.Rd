% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register_from_download.R
\name{register_from_download}
\alias{register_from_download}
\title{Download necessary variants for a font, write a cache entry and
register the font with \code{sysfonts}. Returns the prepared \code{files} list
on success, or \code{NULL} if a regular font could not be obtained.}
\usage{
register_from_download(
  provider,
  name,
  font_id,
  family_name,
  regular.wt = 400,
  bold.wt = 700,
  subset = "latin",
  cache_dir = NULL
)
}
\arguments{
\item{provider}{(\code{FontProvider}) Provider object used for downloads.}

\item{name}{(\code{character(1)}) Font name at the provider.}

\item{font_id}{(\code{character(1)}) Filesystem-safe font id.}

\item{family_name}{(\code{character(1)}) Family name to register the font under.}

\item{regular.wt}{(\code{integer(1)}) Regular weight to fetch (default: 400)}

\item{bold.wt}{(\code{integer(1)}) Bold weight to fetch (default: 700)}

\item{subset}{(\code{character(1)}) Glyph subset to request (default: "latin")}

\item{cache_dir}{(\code{character | NULL}) Cache directory to use (default: NULL)}
}
\value{
(\code{list | NULL}) List of local file paths for variants, or \code{NULL} on failure.
}
\description{
This function downloads fonts, creates a cache entry, and then calls
register_from_cache() to perform the actual registration with sysfonts.
}
